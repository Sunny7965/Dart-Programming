void main() {
  int a = 100;
  final int MAX = 100; //final we use a runtime and compile time works on.
  // if (a < 1) {
  //   MAX = 100;
  // } else {
  //   MAX == 200;
  // }
  const d = 1000; //const we use only compiletime works on.
  print("d is ${d}");
}
